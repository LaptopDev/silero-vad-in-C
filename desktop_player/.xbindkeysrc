# Ctrl+Alt + Button 9 → +5 seconds
"/home/user/system-scripts/seek-anywhere.sh 5"
  Control + Alt + b:9

# Ctrl+Alt + Button 8 → -5 seconds
"/home/user/system-scripts/seek-anywhere.sh -5"
  Control + Alt + b:8

# Ctrl+Alt + WheelUp → dynamic forward scrubbing
"printf '{\"command\":[\"script-message\",\"scrollseek\",\"up\"]}\n' | socat - /tmp/mpvsock"
  Control + Alt + b:4

# Ctrl+Alt + WheelDown → dynamic backward scrubbing
"printf '{\"command\":[\"script-message\",\"scrollseek\",\"down\"]}\n' | socat - /tmp/mpvsock"
  Control + Alt + b:5
